import styled from 'styled-components';

// Define a styled component called 'Title' with CSS properties
export const Title = styled.h1`
    font-size: 2.5rem;
    font-weight: 700;
    text-transform: uppercase;
`