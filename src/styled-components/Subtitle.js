import styled from "styled-components";

// Define a styled component called 'Subtitle' with CSS properties
export const Subtitle = styled.h2`
    font-size: 1.2rem;
    font-weight: 600;
    opacity: 0.4;
    text-transform: uppercase;
`